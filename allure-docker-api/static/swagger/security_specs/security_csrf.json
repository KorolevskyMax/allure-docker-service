{
   "in":"header",
   "name":"X-CSRF-TOKEN",
   "description":"CSRF token from csrf_access_token cookie value. Required when using JWT authentication via cookies. Not required when using API key authentication (X-API-Key header or Authorization: Bearer <api_key>).",
   "schema":{
      "type":"string",
      "format":"uuid"
   },
   "required":"false"
}